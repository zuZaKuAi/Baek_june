#include <iostream>
using namespace std;
int main() {
    int x, y;
    cin >> x >> y;
    if (x > y)
    {
        cout << y * 50;
    }
    else
    {
        cout << x * 50;
    }
    return 0;
}